server:
  port: 7070

  error:
    whitelabel:
      enabled: false

eureka:
  instance:
     hostname: localhost
  client: 
     registrationWithEureka: false
     fetchRegistry: false
     serviceUrl.defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/      